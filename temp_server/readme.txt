This server is for temporary Oauth functionality. 

It will need to be migrated into the python backend when possible(The auth repo would be proper).

Current design will have it run alongside the python backend as a separate system.
